:orphan:

Windows
====================

Python3 (preferível)

1. Clique no link para instalar: `python-3.4.1.msi <https://www.python.org/ftp/python/3.4.1/python-3.4.1.msi>`_
2. Na pasta one salvou o arquivo dê um duplo clique no arquivo para iniciar a instalação. 
3. Siga os passos. Por padrão, o programa será salvo num diretório chamado Python34.
4. Para iniciar o interpretador Python, encontrei o "Python 3.4" no menu iniciar e rode o IDLE
5. (*Opcional*) Uma vez que o interpretador tiver em execução, escolha "File → New Window" para abrir o editor de textos

Python2 (!!!para discussão!!!)

1. Clique no link para instalar: `python-2.7.7.msi <https://www.python.org/ftp/python/2.7.7/python-2.7.7.msi>`_
2. Duplo clique no arquivo para iniciar a instalação. 
3. Siga os passos. Por padrão, o programa será salvo num diretório chamado Python27.
4. Para iniciar o interpretador Python, encontrei o "Python 2.7" no menu iniciar e rode o IDLE
5. (*Opcional*) Uma vez que o interpretador tiver em execução, escolha "File → New Window" para abrir o editor de textos

Linux
====================

**Fedora**

TBA


**Ubuntu**

Python 3 (preferível)

``sudo apt-get install idle-python3.3``

Python 2 (!!!para discussão!!!)

``sudo apt-get install idle-python2.7``

MacOSX
====================

TBA

